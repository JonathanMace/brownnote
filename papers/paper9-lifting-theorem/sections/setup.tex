\section{Mathematical framework}

% Introduce the smooth family $J(\delta)$, the symmetry manifold $\mathcal{M}$,
% the rank-deficient point at $\delta = 0$, and the reduced nullspace coupling
% $B(\delta) = U_0^{*} J(\delta) V_0$.
% TODO: Draft this section
